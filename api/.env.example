# Copy to .env and fill in. Never commit .env.
DATABASE_URL="postgresql://grynx:CHANGE_ME@localhost:5432/grynx?schema=public"
JWT_SECRET="CHANGE_ME_long_random_string"
PORT=4000
# Comma-separated origins allowed to call the API (the PWA on Vercel + local dev)
CORS_ORIGINS="http://localhost:5173,https://grynx.vercel.app"
